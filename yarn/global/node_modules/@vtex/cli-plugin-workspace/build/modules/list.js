"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const chalk_1 = tslib_1.__importDefault(require("chalk"));
const vtex_1 = require("vtex");
exports.default = () => {
    const { account, workspace: currentWorkspace } = vtex_1.SessionManager.getSingleton();
    vtex_1.logger.debug('Listing workspaces');
    const table = vtex_1.createTable({ head: ['Name', 'Weight', 'Production'] });
    const workspaces = vtex_1.createWorkspacesClient();
    return workspaces
        .list(account)
        .then((workspaceArray) => workspaceArray.forEach((workspace) => {
        const name = workspace.name === currentWorkspace ? chalk_1.default.green(`* ${workspace.name}`) : workspace.name;
        const { weight } = workspace;
        const { production } = workspace;
        table.push([name, weight, production]);
    }))
        .then(() => console.log(table.toString()));
};
