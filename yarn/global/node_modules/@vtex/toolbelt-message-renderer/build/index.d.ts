import boxen from 'boxen';
import emojic from 'emojic';
export interface BoxTemplate {
    type: 'box';
    boxConfig: boxen.Options;
    content: string;
}
export interface TextTemplate {
    type: 'text';
    content: string;
}
export declare type NodeToRender = TextTemplate | BoxTemplate;
export declare class TemplateRenderer {
    private mustacheView;
    private static renderer;
    static getDefaultMustacheView(): {
        emoji: typeof emojic;
    };
    static getSingleton(): TemplateRenderer;
    constructor(mustacheView: any);
    renderNode(node: NodeToRender): string;
    renderBox({ boxConfig, content }: BoxTemplate): string;
    renderText(str: string): string;
    private renderColors;
    private renderMustacheTags;
}
